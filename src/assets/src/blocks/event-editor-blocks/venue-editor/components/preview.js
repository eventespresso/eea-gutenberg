/**
 * External imports
 */
import React from 'react';

export default function VenuePreview(props) {
    return (
        <div>
            <p>
                This will display the preview of the venue details as it will appear in the event content in the frontend. Note that the block can also be moved around in the content for the user to control
            </p>
        </div>
    );
}